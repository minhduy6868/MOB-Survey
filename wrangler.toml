name = "trovey"
compatibility_date = "2026-09-01"
pages_build_output_dir = "app/build/web"

# Put SHEETS_WEBHOOK in Cloudflare as a secret, not in this file:
#   npx wrangler pages secret put SHEETS_WEBHOOK --project-name trovey

# Optional KV copy of every ticket. Create with:
#   npx wrangler kv namespace create TROVEY_RECORDS
# then paste the id below.
# [[kv_namespaces]]
# binding = "TROVEY_RECORDS"
# id = "xxxx"
